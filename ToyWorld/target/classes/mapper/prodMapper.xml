<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit.toyworld.dao.ProdMapper">

<insert id="insertProd" parameterType="prod">
insert into 
	toy_product(
		prodnum
		,prodname
		,prodcategory
		,prodmanufacturer
		,prodcontents
		,prodprice
		,prodstock
		,prodboxquantity
		,empid
	<if test="prodOriginalFileName != null">
		,prodoriginalfilename
		,prodsavedfilename
	</if>
	)values(
		#{prodNum}
		,#{prodName}
		,#{prodCategory}
		,#{prodManufacturer}
		,#{prodContents}
		,#{prodPrice}
		,#{prodStock}
		,#{prodBoxQuantity}
		,#{empId}
	<if test="prodOriginalFileName != null">
		,#{prodOriginalFileName}
		,#{prodSavedFileName}
	</if>
	)
</insert>

<select id="allList" resultType="prod">
select
	prodnum
	,prodname
	,prodcategory
	,prodmanufacturer
	,prodcontents
	,prodprice
	,prodstock
	,prodboxquantity
	,prodoriginalfilename
	,prodsavedfilename
	,to_char(prodindate, 'YYYY-MM-DD')
	,empid
from
	toy_product	
</select>

</mapper>
